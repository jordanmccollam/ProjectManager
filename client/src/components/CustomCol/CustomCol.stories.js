import React from 'react';
import CustomCol from './CustomCol';

export default {
  title: 'components/CustomCol',
  component: CustomCol,
  argTypes: {

  }
}

const Template = (args) => <CustomCol {...args} />

export const Default = Template.bind({})
Default.args = {

}

